# Migration `20200411203339-new`

This migration has been generated by Evan Kysley at 4/11/2020, 8:33:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200411201409-new..20200411203339-new
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator typegraphql {
   provider = "node_modules/typegraphql-prisma/generator.js"
@@ -24,10 +24,10 @@
   role              Role      @default(USER)
   updatedAt         DateTime  @default(now())
   username          String    @unique
   usernameLowercase String?   @unique
-  Test              Test[]
-  Trial             Trial[]
+  tests             Test[]
+  trials            Trial[]
 }
 model Test {
   correct     Int
@@ -40,10 +40,10 @@
   type        ResultType
   updatedAt   DateTime   @default(now())
   wordIndex   Int
   wpm         Int
+  trial       Trial?     @relation(fields: [trialId], references: [id])
   trialId     String?
-  trial       Trial?     @relation(fields: [trialId], references: [id])
   account     Account    @relation(fields: [accountId], references: [id])
   accountId   String
 }
@@ -58,10 +58,10 @@
   private       Boolean?    @default(false)
   updatedAt     DateTime    @default(now())
   wordSet       String
   results       Trial[]
+  owner         Account?    @relation(fields: [ownerId], references: [id])
   ownerId       String?
-  owner         Account?    @relation(name: "TrialOwner", fields: [ownerId], references: [id])
 }
 enum ResultType {
   SINGLEPLAYER
```


